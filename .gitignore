.vercel
node_modules
dist
.next
.env
.env.local
.env.*.local

# Excel temp/lock files
~$*.xlsx

# Cursor/Claude — shared launch config only; not machine-local settings
.claude/settings.local.json
.claude/worktrees

# Vite transient timestamp files
vite.config.js.timestamp-*.mjs
