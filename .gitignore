.vercel
node_modules
dist
.next
.env
.env.local
.env.*.local

# Excel temp/lock files
~$*.xlsx

# Cursor/Claude — shared launch config only; not machine-local settings
.claude/settings.local.json
.claude/worktrees
.claude/scheduled_tasks.lock

# TypeScript incremental build info
tsconfig.tsbuildinfo

# Supabase CLI temp files
supabase/.temp/

# Vite transient timestamp files
vite.config.js.timestamp-*.mjs

# Rå foto-kilder (ikke serverede; originaler i OneDrive, serveringsfiler i public/garn-eksempler/)
app/garn/Hannahs fotoeksempler af garn/

# Lokale script-artefakter (sample-data, dev-logs, downloadede swatches)
.cache/

